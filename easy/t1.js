// 1. Create a basic HTTP server

// Respond with plain text: "Hello, world".

// 2. Return JSON

// When the user visits /user, return a JSON object.

// 3. Route based on URL

// Handle:

// -> /

// -> /about

// -> /contact

// 4. Send an HTML page

// Serve an HTML string as the response.

// 5. Read query parameters

// Handle:
// /greet?name=Moises → respond: "Hello Moises".



